# Tests for c-csv library

## Usage

Don't forget to `git submodule update --init --recursive` after cloning.


Use `./run.sh` for a basic execution.

Use `./val_debug.sh` for [valgrind](https://valgrind.org/) debug.
